<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <title>Edit</title>
</head>
<body>
    <%- include('partials/header') %>
  <div class="container-lg">
    <h1 class="text-center my-3"><%= book.name %></h1>
    <form action="/<%= book.id %>?_method=PUT" method="post" class="row g-4" id="bookEditForm">
        <div class="form-group col-md-12">
            <label for="bookName" class="form-label required">Book Name</label>
            <input type="text" class="form-control" id="bookName" name="name" value="<%= book.name %>" required>
        </div>
        <div class="form-group col-md-12">
            <label for="bookId" class="form-label required">Book ID</label>
            <input type="text" class="form-control" id="bookId" name="id" value="<%= book.id %>" readonly>
        </div>
        <div class="form-group col-md-12">
            <label for="bookCategory" class="form-label required">Book Category</label>
            <select class="form-select" id="bookCategory" name="category" required>
                <option selected disabled value=""><%= book.category %> </option>
                <option value="Romance" <%= book.category === 'Romance' ? 'selected' : '' %>>Romance</option>
                <option value="Finance" <%= book.category === 'Finance' ? 'selected' : '' %>>Finance</option>
                <option value="Horror" <%= book.category === 'Horror' ? 'selected' : '' %>>Horror</option>
                <option value="Motivational" <%= book.category === 'Motivational' ? 'selected' : '' %>>Motivational</option>
                <option value="Inspirational" <%= book.category === 'Inspirational' ? 'selected' : '' %>>Inspirational</option>
            </select>
        </div>
        <div class="col-md-6">
            <label for="bookPrice" class="form-label required">Book Price</label>
            <input type="text" class="form-control" id="bookPrice" name="price" value="<%= book.price %>" required>
        </div>
        <div class="col-md-6">
            <label for="quantity" class="form-label required">Quantity</label>
            <input type="number" class="form-control" id="quantity" name="quantity" value="<%= book.quantity %>" required>
        </div>
        <div class="col-md-12">
            <label for="bookDescription" class="form-label">Description of the book</label>
            <textarea class="form-control" id="bookDescription" name="description" style="height: 100px;"><%= book.description %></textarea>
        </div>
        <div class="col-md-12 text-center">
            <!-- Button to trigger the modal -->
            <button type="button" class="btn btn-primary btn-lg" data-bs-toggle="modal" data-bs-target="#deleteModal">Save</button>
            <a href="/" class="btn btn-lg btn-secondary text-white">Back</a>
        </div>
    </form>        
  </div>

  <!-- Modal Tab-->
  <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Edit Data Confirmation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to change the information of this book?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="submit" class="btn btn-success" form="bookEditForm">Save</button>
            </div>
        </div>
    </div>
  </div>

  <%- include('partials/footer') %>
  
  <script src="/scripts/edit.js"></script>
  <script src="/scripts/bootstrap.bundle.min.js"></script>
</body>
</html>
